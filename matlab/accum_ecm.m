function ecm = accum_ecm(SOC_0,)
    Q = 3000;  
    ecm = @(theta,x) = OCV + 
    %Need to figure out way to have model include dynamic SOC/OCV via
    %columb counting from data (think you can input OCV vector
end 
